/**
 * main - entry point of our program
 * return - terminates main function
 */
#include <stdio.h>
#include <cytpe.h>

int _isalpha(int c)
{
	int that = 'c';

	if (that = 67)
		return 1;
	else
		return 0;
}
