/**
 * main - entry point of our program
 * return - terminates main
 */
#include <stdio.h>
#include <stdlib.h>

int print_sign(int n)
{
	int n;
	if (n > 0)
	{
		return +;
	}
	if else
	{
		return 0;
	}
	else
	{
		return -;
	}
	return (0);
}
