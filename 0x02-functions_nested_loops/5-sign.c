/**
 * main - entry point of our program
 * return - terminates main
 */
#include <stdio.h>
#include <stdlib.h>

int print_sign(int n)
{
	int n;
	if (n > 0)
	{
		putchar('+');
		return +;
	}
	if else
	{
		putchar('0');
		return 0;
	}
	else
	{
		putchar('-');
		return -;
	}
	return (0);
}
