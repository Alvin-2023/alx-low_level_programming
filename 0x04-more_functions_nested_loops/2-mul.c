#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * mul - returns product of two integers
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int product;
	product = a * b;
	return (product);
}
